services:
  shideo:
    build:
      context: .
      dockerfile: Shideo.Dockerfile
    container_name: shideo
    volumes:
      - ..:/workspaces:cached
    ports:
      - "5173:5173"

  pocketbase:
    build:
      context: .
      dockerfile: Pocketbase.Dockerfile
    container_name: pocketbase
    ports:
      - "8080:8080"
